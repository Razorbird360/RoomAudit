# ── Install PyTorch manually first (RTX 5070 Ti requires CUDA 12.8+) ──────────
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# ── Then install Unsloth ───────────────────────────────────────────────────────
# pip install unsloth
#
# ── Then install flash-attn (optional but recommended for Qwen2.5-VL speed) ───
# pip install flash-attn --no-build-isolation
#
# ── Then install everything else ──────────────────────────────────────────────

# HuggingFace ecosystem
datasets>=3.0.0
huggingface-hub>=0.24.0
transformers>=4.49.0
accelerate>=1.0.0
diffusers>=0.32.0
peft>=0.14.0
trl>=0.15.0

# Qwen2.5-VL utilities
qwen-vl-utils>=0.0.8

# SAM (segment anything) — for auto-masking pillows/bedsheets/toilets
# Use SAM2 if preferred: pip install sam2
segment-anything

# Image processing
Pillow>=10.4.0
opencv-python>=4.10.0
numpy>=1.26.0

# Quantization (for 4-bit LoRA via Unsloth)
bitsandbytes>=0.45.0

# Utilities
tqdm>=4.66.0
requests>=2.32.0
sentencepiece>=0.2.0
